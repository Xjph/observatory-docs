# Observatory Framework

The Observatory Framework library contains all interfaces and types used for interoperation between Observatory Core and its plugins within the `Observatory.Framework` namespace.

